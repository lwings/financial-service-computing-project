#include "Order.h"
#include <vector>
#include <cstring>

Order parse_command(std::string &str, int &success)
{
    int length = str.length();
    int i = 0;
    for(;i<length;++i)
    {
        if(str[i]==' ')
        {

        }
    }


    return Order();
}